{
  "name": "claude-hybrid-router",
  "private": true,
  "description": "HTTP entry; run npm start from repository root (npm test runs there too).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": { "node": ">=18" }
}
